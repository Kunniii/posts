---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE, REPO } from '../consts';
---

<header class="flex flex-col">
  <h2 class="mx-auto my-8">
    {SITE_TITLE}
  </h2>
  <nav class="mx-auto">
    <HeaderLink href={REPO}>Home</HeaderLink>
    <HeaderLink href={`${REPO}/blog`}>Blog</HeaderLink>
    <HeaderLink href={`${REPO}/about`}>About</HeaderLink>
  </nav>
  <hr class="my-5">
</header>
